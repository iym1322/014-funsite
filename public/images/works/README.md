曲ごとの画像はこのフォルダに置く。

- ファイル名は `discography.ts` の各曲の `image` フィールドと一致させる(拡張子は .jpg / .jpeg / .png / .webp のいずれか)
  例: `image: "kimi-janakya-dame-mitai"` → `kimi-janakya-dame-mitai.jpg` を配置
- 画像がまだ無い曲はプレースホルダー表示になる
- ジャケット画像はルートの README.md「開発ルール」の例外規定により、無断で配置してよい(歌詞・アーティスト写真等は引き続き制限対象)
